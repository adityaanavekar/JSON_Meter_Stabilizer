# JSON_Meter_Stabilizer
Embedded-friendly C++ JSON serialization library for smart-meter data, designed for ESP32 (Arduino). Converts structured meter data into a fixed JSON format without using any external JSON libraries.
